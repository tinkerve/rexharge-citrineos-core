{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "rootDir": "./",
    "outDir": "./dist"
  },
  "exclude": [
    "./dist/**/*",
    "**/node_modules/**",
    "DirectusExtensions", // todo for now
    "Swarm", // todo for now
    "migrations" // lack of sequelize support for ESM, have to build separately
  ],
  "references": [
    {
      "path": "./Server"
    }
  ]
}
